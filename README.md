All scripts are C# and are made to be used with unity
DebugPanel.cs
The script only needs to be placed on any object to work.

` will open and close the panel
all arguments are seperated by spaces
; after a command will let you place another one afterwards so its kinda a makeshift programming language
